# This file contains dependency overrides for the runner_background package.
# It allows the package to use newer versions of dependencies when available.

dependency_overrides:
  # Override flutter_local_notifications version
  flutter_local_notifications: ^19.3.1

  # Override workmanager version
  workmanager: ^0.8.0

  # Override shared_preferences version
  shared_preferences: ^2.4.10

  # Override timezone version
  timezone: ^0.10.1
